/**
 * WRITE OPERATIONS — confirm-before-write guidance, added only when the
 * role can write. Prompt text is not shipped in this distribution;
 * provide your own.
 */
export const WRITE_OPERATIONS = "";
