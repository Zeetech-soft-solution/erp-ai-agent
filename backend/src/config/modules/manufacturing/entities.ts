import { EntityConfig } from "../../../core/types";

/** Manufacturing module. Free tier: empty. This file stays here,
 *  empty, so the module's folder shape (entities.ts, rules.ts,
 *  training.ts) is complete and consistent — real coverage (bom,
 *  work_order, and the rest) is a pro-tier capability. */
export const MANUFACTURING_ENTITIES: EntityConfig[] = [];
