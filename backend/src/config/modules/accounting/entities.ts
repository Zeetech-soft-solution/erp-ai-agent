import { EntityConfig } from "../../../core/types";

/** Accounting module. Free tier: empty. This file stays here, empty,
 *  so the module's folder shape (entities.ts, rules.ts, training.ts)
 *  is complete and consistent — real coverage (journal_entry,
 *  payment_entry, and the rest) is a pro-tier capability. */
export const ACCOUNTING_ENTITIES: EntityConfig[] = [];
