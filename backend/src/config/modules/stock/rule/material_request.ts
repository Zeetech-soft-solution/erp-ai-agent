import { RuleSet } from "../../../../core/types";

/**
 * Material request business rules.
 * Rule definitions are not shipped in this distribution; add your own.
 */
export const MATERIAL_REQUEST_RULES: RuleSet = {
  entityKey: "material_request",
  rules: [],
};
