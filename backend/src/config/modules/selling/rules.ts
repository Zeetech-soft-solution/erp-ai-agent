import { RuleSet } from "../../../core/types";

/**
 * Selling module rules — empty for now. Follow crm/rules.ts for the
 * pattern once this module gets real entity coverage.
 */
export const SELLING_RULES: RuleSet[] = [];
