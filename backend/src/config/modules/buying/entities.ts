import { EntityConfig } from "../../../core/types";

/** Buying module — procurement side, mirrors Selling's shape. Free
 *  tier: empty. This file stays here, empty, so the module's folder
 *  shape (entities.ts, rules.ts, training.ts) is complete and
 *  consistent — real coverage (supplier, purchase_order,
 *  purchase_invoice, and the rest) is a pro-tier capability. */
export const BUYING_ENTITIES: EntityConfig[] = [];
