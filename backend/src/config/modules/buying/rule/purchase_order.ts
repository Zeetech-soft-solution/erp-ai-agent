import { RuleSet } from "../../../../core/types";

/**
 * Purchase order business rules.
 * Rule definitions are not shipped in this distribution; add your own.
 */
export const PURCHASE_ORDER_RULES: RuleSet = {
  entityKey: "purchase_order",
  rules: [],
};
