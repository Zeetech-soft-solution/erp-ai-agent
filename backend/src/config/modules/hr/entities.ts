import { EntityConfig } from "../../../core/types";

/** HR module. Free tier: empty. This file stays here, empty, so the
 *  module's folder shape (entities.ts, rules.ts, training.ts) is
 *  complete and consistent — real coverage (employee,
 *  leave_application, and the rest) is a pro-tier capability. */
export const HR_ENTITIES: EntityConfig[] = [];
