import { RuleSet } from "../../../../core/types";

/**
 * Training event business rules.
 * Rule definitions are not shipped in this distribution; add your own.
 */
export const TRAINING_EVENT_RULES: RuleSet = {
  entityKey: "training_event",
  rules: [],
};
