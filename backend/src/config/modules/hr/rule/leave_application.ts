import { RuleSet } from "../../../../core/types";

/**
 * Leave application business rules.
 * Rule definitions are not shipped in this distribution; add your own.
 */
export const LEAVE_APPLICATION_RULES: RuleSet = {
  entityKey: "leave_application",
  rules: [],
};
