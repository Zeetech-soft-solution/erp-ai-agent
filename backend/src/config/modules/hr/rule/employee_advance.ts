import { RuleSet } from "../../../../core/types";

/**
 * Employee advance business rules.
 * Rule definitions are not shipped in this distribution; add your own.
 */
export const EMPLOYEE_ADVANCE_RULES: RuleSet = {
  entityKey: "employee_advance",
  rules: [],
};
