import { RuleSet } from "../../../../core/types";

/**
 * Leave allocation business rules.
 * Rule definitions are not shipped in this distribution; add your own.
 */
export const LEAVE_ALLOCATION_RULES: RuleSet = {
  entityKey: "leave_allocation",
  rules: [],
};
