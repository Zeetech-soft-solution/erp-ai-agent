import { RuleSet } from "../../../../core/types";

/**
 * Shift assignment business rules.
 * Rule definitions are not shipped in this distribution; add your own.
 */
export const SHIFT_ASSIGNMENT_RULES: RuleSet = {
  entityKey: "shift_assignment",
  rules: [],
};
