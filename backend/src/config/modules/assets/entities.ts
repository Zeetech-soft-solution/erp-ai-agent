import { EntityConfig } from "../../../core/types";

/** Assets module — fixed asset tracking and maintenance. Free tier:
 *  empty. This file stays here, empty, so the module's folder shape
 *  (entities.ts, rules.ts, training.ts) is complete and consistent —
 *  real coverage (asset, asset_maintenance, and the rest) is a
 *  pro-tier capability. */
export const ASSETS_ENTITIES: EntityConfig[] = [];
