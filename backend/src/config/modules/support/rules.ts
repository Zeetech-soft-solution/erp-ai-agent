import { RuleSet } from "../../../core/types";

/**
 * Support module rules — stub. No business rules populated yet; this
 * file exists so the module's folder shape (entities.ts, rules.ts,
 * training.ts) is complete and consistent from day one, ready for
 * pro-tier detail. Follow crm/rules.ts or selling/rules.ts for the
 * pattern once this module gets real coverage.
 */
export const SUPPORT_RULES: RuleSet[] = [];
