import { EntityConfig } from "../../../core/types";

/** Support module — customer support tickets (ERPNext's Issue doctype).
 *  Free tier: empty. This file stays here, empty, so the module's folder
 *  shape (entities.ts, rules.ts, training.ts) is complete and consistent
 *  — real coverage (issue, and the rest) is a pro-tier capability. */
export const SUPPORT_ENTITIES: EntityConfig[] = [];
