import { EntityConfig } from "../../../core/types";

/** Quality Management module — inspections and quality process
 *  tracking. Free tier: empty. This file stays here, empty, so the
 *  module's folder shape (entities.ts, rules.ts, training.ts) is
 *  complete and consistent — real coverage (quality_inspection,
 *  quality_goal, and the rest) is a pro-tier capability. */
export const QUALITY_ENTITIES: EntityConfig[] = [];
