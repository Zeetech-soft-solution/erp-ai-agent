import { RuleSet } from "../../../../core/types";

/**
 * Quality inspection business rules.
 * Rule definitions are not shipped in this distribution; add your own.
 */
export const QUALITY_INSPECTION_RULES: RuleSet = {
  entityKey: "quality_inspection",
  rules: [],
};
