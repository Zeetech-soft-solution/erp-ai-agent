import { EntityConfig } from "../../../core/types";

/** Projects module. Free tier: empty. This file stays here, empty, so
 *  the module's folder shape (entities.ts, rules.ts, training.ts) is
 *  complete and consistent — real coverage (project, task, timesheet)
 *  is a pro-tier capability. */
export const PROJECTS_ENTITIES: EntityConfig[] = [];
